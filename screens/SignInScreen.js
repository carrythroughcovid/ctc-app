import React from "react";

import SignInTabView from '../components/SignInTabView';

const SignInScreen = ({ navigation }) => {
  return (
    <SignInTabView navigation={navigation} />
  )
}

export default SignInScreen;